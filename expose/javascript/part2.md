1.3
2.150
3. 150
4. it would return [50,100,150]. This is becuase it multiplies the originla values by .5 and then appends to array
5. This will cause an error because using let means it is no longer in the scope  after the loop.
6. Error discounted price is only called in the scope of the loop
7. it will print 150 because final price stores the last discounted value in the array
8.  it would return [50,100,150]. This is becuase it multiplies the originla values by .5 and then appends to array
9.  Error i is not defined becuase it is not in scope
10. 3 because the length of prices is 3
11. It will return [50,100,150]it loops through the array and stores the value in the array * .5 then appends to array
12. A) student.name B)student['Grad Year']C)students.greeting() D)student['Favorite Teacher'].name E)students.courseLoad[0]
13. '32' (the 2 turns into a string), 1 (3 turns into a number so 3-2), '3null' ( the null is turned into a string), 4 (true also means 1, 1+3 = 4), 0 (null and false map to 0, 0+0), '3undefined' (undefined turns into a string), NaN ('3' turns into 3 and undefined turns into NaN which if used turns into NaN)
14. True ('2' gets turned into a number), false(no number value in string cant compare this way), false (not the same type), false(not the same type), false (true turn to 1, but 1 !=2), True( Boolean(2) is true and true===true)
17. [2,4,6] FOr each element in arary it calls doSomething() method in each value then appends the array. 
19. 1 3 4 2
